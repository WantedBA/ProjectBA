# Agent Workflow Notes

- 구현 작업은 검토 가능한 기능 단위로 나눠 커밋한다.
- 하나의 커밋은 독립적으로 이해 가능한 사용자 관점의 변경 하나를 담는다. 파일 수가 많더라도 같은 기능/버그 수정에 속하면 한 커밋으로 묶고, 단순히 파일 두세 개 단위로 쪼개지 않는다.
- C++ 보강, 데이터 수정, 에디터 에셋 수정처럼 성격이 명확히 다른 작업만 커밋을 분리한다.
- 사용자가 만든 변경이나 무관한 생성물은 에이전트 커밋에 섞지 않는다.
- 커밋 전에는 staged diff를 확인하고, 해당 단계에 속한 파일만 스테이징한다.
- 커밋 메시지는 한글로 작성하고, 파일명이 아니라 동작이나 데이터 변경 의도를 설명한다.
- 작업 도중에는 검토와 되돌리기 편의를 위해 세부 커밋을 남길 수 있지만, 원격 push 전에는 사용자의 지시에 따라 interactive rebase로 관련 커밋을 큰 작업 단위로 합쳐 이력을 정리한다.
- push 전 커밋 정리를 할 때는 합칠 커밋 범위와 최종 커밋 구성을 먼저 확인하고, 기존 세부 커밋 메시지의 핵심 문구는 최종 커밋 메시지 제목이나 본문에 보존한다.
- 기능 변경/데이터 변경/에디터 에셋 변경처럼 성격이 다른 작업은 한 작업 단위에 꼭 함께 들어가야 하는 경우가 아니면 별도 커밋으로 유지한다.

## Code Readability Guidance

- 사용자가 가독성 좋은 구조를 요청하면 이벤트/라이프사이클 함수에는 의도를 드러내는 상위 함수 한두 개만 호출하게 하고, 세부 구현은 작은 함수들로 분리한다.
- 예: `OnDeath()`에서는 `DropWeaponAndDie()`처럼 사용자 관점의 의미가 드러나는 함수를 호출하고, 그 내부에서 상태 정리, 정책 판단, 실제 처리, 후속 연출을 순서대로 나눈다.
- 조건 판단 함수(`Should...`, `Can...`)와 실제 실행 함수(`Drop...`, `Apply...`, `Play...`)를 분리해, 지금은 자동으로 실행되더라도 나중에 애니메이션 노티파이/BP/다른 타이밍에서 같은 실행 함수를 재사용할 수 있게 둔다.
- 함수명은 파일명이나 구현 방식보다 게임플레이 의도와 호출 타이밍을 설명하게 짓는다.

## Unreal Editor Guidance

- 언리얼 에디터에서 사용자가 직접 해야 하는 작업이 있으면, 에이전트는 사용자를 단계별로 지휘하듯 구체적으로 안내한다.
- 안내는 현재 위치, 클릭할 메뉴/버튼, 생성할 에셋 이름, 저장 위치, 설정값, 검증 방법을 포함한다.
- 한 번에 너무 많은 작업을 던지지 말고, 에디터에서 확인 가능한 체크포인트 단위로 나눠 안내한다.
- 사용자가 에디터에서 수행한 뒤 알려줘야 하는 결과를 명확히 말한다.
- 에디터 작업으로 생성/수정된 에셋은 이후 `git status`로 확인하고, 에이전트가 커밋할 때는 관련 에셋만 별도 커밋으로 묶는다.
