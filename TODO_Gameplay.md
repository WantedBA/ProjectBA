# 게임플레이 할일 목록

작성일: 2026-05-27
현재 브랜치: `fix/gamepad-input`
기준 브랜치: `develop`

정책, 결정사항, 현재 동작 요약은 [GAMEPLAY_POLICY.md](GAMEPLAY_POLICY.md)에만 기록한다. 이 문서는 진행 중 작업과 남은 할일만 기록한다.

## 남은 작업

### 1. 회피 이동거리 직접 제어

- 대쉬와 구르기의 체감 거리가 달라서 회피 이동거리를 코드/데이터로 직접 제어하는 방향을 검토한다.
- 루트모션 의존을 줄이고, 구르기 중에는 긴 이동거리, 착지 후 리커버리 구간에는 짧은 이동거리를 적용해 대쉬와 구르기 총 거리를 맞춘다.
- 백스텝도 같은 방식으로 직접 제어하되, 기본 목표 거리는 구르기 거리의 절반으로 잡는다.
- 몽타주 루트모션 사용 여부와 `ActionAnimationData` 설정을 함께 확인해야 한다.

### 2. 체크포인트 상호작용 흐름 단순화

- 현재는 상호작용 직후 스킬트리가 바로 열리고, 스킬트리에서 나간 뒤 다시 상호작용해야 체크포인트를 빠져나오는 흐름이다.
- 목표 흐름:
  - E/A 입력 시 앉기 몽타주 재생
  - 화면 페이드아웃
  - 스킬트리 표시
  - B/Esc 입력 시 스킬트리 닫기
  - 화면 페이드인
  - 서기 몽타주 재생
- 앉기/서기 몽타주 에셋 참조 위치와 스킬트리 닫힘 콜백 연결 지점을 먼저 확인한다.

### 3. 구르기 시작 구간 모션 반복 버그

- 구르기 애니메이션 시작 구간에서 모션이 여러 번 반복되는 현상이 있다.
- 입력 버퍼 소비, 후딜 탈출 윈도우, 몽타주 재시작 조건이 중복으로 실행되는지 확인한다.

## 이번 Push 완료 목록

### 게임패드 입력과 UI 탐색

- 체력 회복을 게임패드 Y 버튼에 매핑했다.
- 타이틀 UI에서 게임패드 스틱 이동과 A/B 선택/취소 입력을 복구했다.
- 스킬트리 UI에서 게임패드 스틱으로 노드를 탐색하고, 기존 마우스 hover/click 피드백과 툴팁 흐름을 재사용하도록 정리했다.
- 스틱 탐색 방향 보정, 감도 조정, helper 함수 이름 충돌을 수정했다.

### 입력 버퍼와 후딜 탈출

- 후딜 탈출 입력은 윈도우가 열리기 전에 저장되고, 윈도우가 열릴 때 소비되도록 정리했다.
- 입력 버퍼는 마지막 입력 1개만 유지하는 방식으로 정리했다.
- 대쉬 스킬로 구르기 몽타주가 바뀐 경우에도 후딜 탈출 윈도우가 동작하도록 수정했다.
- 백스텝, 구르기, 회피 계열의 후딜 탈출 판정을 같은 명령 흐름에서 처리하도록 보강했다.
- 구르기/회피 연속 2회 캔슬 후 정상 종료되면 다음 체인의 캔슬 횟수가 초기화되도록 수정했다.

### 모션 중 상태 제어

- 리스폰 모션 재생 중 이동을 막았다.
- 체력 회복 모션 중에는 Walk 상태로 고정했다.
- 작은 단차 낙하에서 FallLoop가 남발되지 않도록 최소 낙하 거리 조건을 추가했다.
- 구르기/회피 연속 캔슬은 2회까지만 허용하는 기본 제한을 추가했다.

### 로딩 화면

- 새 게임/컨티뉴로 맵을 불러올 때 회색 미완성 화면이 노출되지 않도록 맵 전환 중 검은 로딩 화면을 표시했다.
